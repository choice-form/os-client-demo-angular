import { DynamicNodeAnchorDirective } from './dynamic-node-anchor.directive';

describe('DynamicNodeAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicNodeAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
